# Example environment variables (for non-Lovable environments)
# NOTE: Lovable does not support runtime env vars in code. The app uses the configured Supabase URL and anon key directly.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
